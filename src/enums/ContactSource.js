const ContactSource = {
    PLATFORM: 'platform',   // Inscription via la plateforme (compte utilisateur)
    FOOTER: 'footer',       // Inscription via le footer du site
    BATCH: 'batch',         // Ajout manuel via un import batch (campagnes promotionnelles)
  };
  
  export default ContactSource;
  